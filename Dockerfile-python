# Dockerfile

FROM python:slim

ENV TZ Asia/Seoul
RUN ln -snf /usr/share/zoneinfo/$TZ /etc/localtime && echo $TZ > /etc/timezone

RUN apt-get install git curl openssl ca-certificates && \
    pip install awscli
